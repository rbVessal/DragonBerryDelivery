package code {		import flash.display.SimpleButton;	import flash.events.MouseEvent;	//import code.Document;			public class PlayButton extends SimpleButton 	{		//Create a private instance of document		private var document:Document;				public function PlayButton() 		{			cacheAsBitmap = true;			// constructor code			addEventListener(MouseEvent.CLICK, goToGame);		}				public function goToGame(e:MouseEvent):void		{			(parent as Document).playGame();			(parent as Document).gotoAndStop("Games");			//trace(Document);			//document = new Document();			//trace(document);			//document.Games.begin();								}	}	}