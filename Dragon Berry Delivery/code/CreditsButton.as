package code {	//@author: Rebecca Vessal	//Instructor: Professor Jefferson	//Date: 29 October 2011	//	//CreditsButton.as represents the button for going to credits screen.	//	import flash.display.SimpleButton;	import flash.events.MouseEvent;			public class CreditsButton extends SimpleButton 	{						public function CreditsButton() 		{			cacheAsBitmap = true;			// constructor code			addEventListener(MouseEvent.CLICK, goToCredits);		}				public function goToCredits(e:MouseEvent):void		{			//Remove game elements from the screen.			if((parent as Document).Games.gameLayerArray.length > 0)			{				(parent as Document).Games.resetGame();			}			(parent as Document).gotoAndStop("Credits");			removeEventListener(MouseEvent.CLICK, goToCredits);		}	}	}